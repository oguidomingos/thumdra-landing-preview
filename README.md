# Thumdra — preview das variações da landing (Hero + Features)

Build estático gerado a partir da branch `preview/landing-redesign-pages`, só para revisão visual.
Alternar: `?hero=0..4&features=0..3` (0 = versão atual) ou pela barra fixa no rodapé.
